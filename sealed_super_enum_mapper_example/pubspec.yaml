name: sealed_super_enum_mapper_example
description: map super_enum manifest to dart_sealed manifest examples.
version: 0.1.0
homepage: https://github.com/6thsolution/dart_sealed
publish_to: 'none'

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  sealed_annotations:
    path: ../sealed_annotations

dev_dependencies:
  sealed_super_enum_mapper:
    path: ../sealed_super_enum_mapper
  pedantic: ^1.9.0
  test: ^1.16.8
  build_runner: ^1.12.2
